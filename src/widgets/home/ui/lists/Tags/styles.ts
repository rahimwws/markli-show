import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  row: {
    flexDirection: 'row',
    flexWrap: 'wrap',
    padding: 16,
    margin: -4.5,
  },
  col: {
    flex: 1,
    padding: 4.5,
  },
});

export default styles;
